services:
  - type: web
    name: mini-jira-backend
    env: node
    repo: https://github.com/Milind74/Bug-Tracker.git
    rootDir: ./bug-tracker-server
    buildCommand: mkdir -p dist && cp fallback.js dist/index.js
    startCommand: node dist/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: trueype: web
    name: mini-jira-backend
    env: node
    repo: https://github.com/Milind74/Bug-Tracker.git
    rootDir: ./bug-tracker-server
    buildCommand: npm ci --include=dev && npm run build
    startCommand: node dist/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true